# 依赖和构建产物由镜像内部重新生成，避免构建上下文过大。
node_modules
dist
dist-ssr

# Git 与编辑器文件不参与镜像构建。
.git
.idea
.vscode

# 日志和本地环境文件禁止进入镜像，防止泄露本机配置。
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.env
.env.*
*.local

# OTF 字体体积过大，生产包仅使用 woff2 字体。
src/assets/fonts/*.otf
