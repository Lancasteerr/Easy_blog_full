# 前端 Docker / nginx 部署流程

本文档说明如何通过 GitHub Actions 把 `blog_front` 打包成 GHCR 镜像，并在生产服务器拉取镜像后通过 nginx 对外服务前端页面，同时同源反代 `/api` 和 `/files` 到后端容器。

## 部署目标

- 前端生产入口统一由 nginx 提供，页面、接口、上传图片都走同一个域名。
- 开发环境继续使用 Vite 热更新，`/api` 代理到本机后端 `http://localhost:5090`。
- 正式环境中后端不直接暴露公网端口，只允许前端 nginx 通过 Docker Compose 内部网络访问。

## 一、准备环境变量

进入单仓库中的后端目录：

```powershell
cd E:\codes\java_class_work\demo\Easy_blog_full\demo_bk
Copy-Item .env.prod.example .env.prod
```

编辑 `.env.prod`，至少确认这些值：

```properties
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=5090
FRONTEND_HTTP_PORT=80
BLOG_BACKEND_IMAGE=ghcr.io/lancasteerr/easy-blog-backend:prod
BLOG_FRONTEND_IMAGE=ghcr.io/lancasteerr/easy-blog-frontend:prod
BLOG_CORS_ALLOWED_ORIGINS=https://你的域名
BLOG_FILE_DOMAIN=https://你的域名/files
BLOG_JWT_SECRET=替换为随机生成的base64密钥
BLOG_DB_URL=jdbc:mysql://mysql:3306/white_jotter?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_general_ci&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&connectTimeout=3000&socketTimeout=10000&tcpKeepAlive=true&rewriteBatchedStatements=true
BLOG_DB_USERNAME=blog_user
BLOG_DB_PASSWORD=替换为强密码
MYSQL_ROOT_PASSWORD=替换为强密码
BLOG_REDIS_HOST=redis
BLOG_REDIS_PASSWORD=替换为强密码
```

如果只是本机测试，没有 HTTPS 域名，可以临时改成：

```properties
BLOG_CORS_ALLOWED_ORIGINS=http://localhost
BLOG_FILE_DOMAIN=http://localhost/files
```

## 二、拉取镜像并启动

前端和后端镜像由 GitHub Actions 构建并推送到 GHCR。生产服务器从 `demo_bk` 目录执行：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml pull
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml up -d
```

查看容器状态：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml ps
```

查看前端 nginx 日志：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml logs -f frontend
```

## 三、验证访问

浏览器访问：

- `http://localhost/`
- `http://localhost/article/1`
- `http://localhost/neko-panel/login`

接口验证：

```powershell
Invoke-WebRequest http://localhost/api/public/get_article_list?page=1&size=5
```

如果已经有文章图片，验证图片代理：

```powershell
Invoke-WebRequest http://localhost/files/你的图片路径
```

刷新 `/article/1` 或 `/neko-panel/login` 不应出现 nginx 404；如果出现 404，优先检查前端镜像是否使用了 `nginx/default.conf` 中的 `try_files` 配置。

## 四、常见问题

### 1. 页面能打开，但接口 502

检查后端是否启动成功：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml logs -f backend
```

重点看数据库、Redis、JWT 密钥和端口配置。前端 nginx 会把 `/api` 反代到 `backend:5090`，所以后端容器内端口必须与 `SERVER_PORT` 一致。

### 2. 登录或上传接口跨域失败

同源反代正常情况下不应跨域。若浏览器仍报告 CORS，检查：

- 前端 `VITE_API_BASE_URL` 是否仍为 `/api`。
- 访问页面的域名是否和 `BLOG_CORS_ALLOWED_ORIGINS` 完全一致。
- 是否绕过 nginx 直接访问了后端端口。

### 3. 上传文件失败或 413

nginx 当前限制为 `client_max_body_size 6m`，后端示例配置为 `BLOG_UPLOAD_MAX_REQUEST_SIZE=6MB`。如需上传更大文件，要同时调整 nginx 配置和后端环境变量。

### 4. 上传成功但图片访问 404

上传接口返回成功后，图片会保存到后端容器的 `/app/blog-storage`，并由 Docker named volume 持久化；不会保存到源码目录 `demo_bk/blog-storage`。

先确认文件是否已经进入后端容器：

```powershell
docker exec easy-blog-prod-backend-1 find /app/blog-storage -maxdepth 6 -type f
```

如果文件存在但浏览器访问 `/files/...jpg` 返回 404，优先检查前端 nginx 镜像是否已经包含 `location ^~ /files/` 配置，并重新拉取最新前端镜像：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml pull frontend
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml up -d frontend
```

### 5. 静态资源更新后浏览器仍显示旧页面

前端 hash 资源会长期缓存，`index.html` 不强缓存。重新部署后刷新页面即可拿到新资源；如果仍异常，可以在浏览器强制刷新一次。

## 五、回滚

如果新版本异常，可以把 `.env.prod` 中的镜像改为上一版 SHA 标签：

```properties
BLOG_BACKEND_IMAGE=ghcr.io/lancasteerr/easy-blog-backend:sha-xxxxxxx
BLOG_FRONTEND_IMAGE=ghcr.io/lancasteerr/easy-blog-frontend:sha-xxxxxxx
```

然后重新拉取并启动：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml pull
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml up -d
```

如果只是临时停止前端：

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml stop frontend
```

恢复 `prod` 标签后重新部署：

```properties
BLOG_BACKEND_IMAGE=ghcr.io/lancasteerr/easy-blog-backend:prod
BLOG_FRONTEND_IMAGE=ghcr.io/lancasteerr/easy-blog-frontend:prod
```

```powershell
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml pull
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml up -d
```

## 六、上线前检查清单

- `npm run build` 能成功完成。
- GitHub Actions 的 Docker 镜像构建任务成功完成。
- `npm audit --registry=https://registry.npmjs.org` 显示无高危漏洞。
- `/`、`/article/1`、`/neko-panel/login` 刷新不 404。
- `/api/public/get_article_list` 通过 nginx 能访问。
- 管理页登录后能发布文章和上传图片。
- `.env.prod` 没有提交到 Git，数据库、Redis、JWT 都使用生产强密码。
