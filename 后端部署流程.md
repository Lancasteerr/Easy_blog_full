# 后端部署流程

本文档用于记录单仓库中的 `demo_bk` 后端从本地开发配置到 Docker 正式部署的流程。后端项目目录为 `demo_bk`，后端相关 Maven 和 Docker 命令默认在该目录内执行。

## 1. 配置文件关系

- `src/main/resources/application.yml`：公共配置，包含应用名、端口、上传大小、MyBatis Plus、Redis 连接池等通用项。
- `src/main/resources/application-dev.yml`：开发环境配置，默认连接本机 MySQL/Redis，允许 `http://localhost:8080` 前端访问。
- `src/main/resources/application-prod.yml`：部署环境配置，只从环境变量读取数据库、Redis、JWT、CORS、文件域名等关键值。
- `.env.dev.example`：本地开发环境变量模板。
- `.env.prod.example`：正式部署环境变量模板。
- `docker-compose.dev.yml`：只启动本机开发用 MySQL 和 Redis，后端仍建议用 Maven/IDEA 跑。
- `docker-compose.prod.yml`：开发机完整构建测试用，会在本机执行前后端镜像构建。
- `docker-compose.prod.pull.yml`：生产服务器专用，只拉取 GHCR 镜像并启动服务，不在2核2G服务器上构建。
- `.github/workflows/docker-images.yml`：推送到 `main` 后由 GitHub Actions 构建前后端镜像并推送到 GHCR。

真实 `.env`、`.env.dev`、`.env.prod` 都不要提交到 Git。

## 2. 本地开发流程

进入后端目录：

```powershell
cd E:\codes\java_class_work\demo\Easy_blog_full\demo_bk
```

复制开发环境变量：

```powershell
Copy-Item .env.dev.example .env.dev
```

按需修改 `.env.dev`，至少确认数据库密码、Redis 密码、端口没有冲突。启动开发依赖：

```powershell
docker compose --env-file .env.dev -f docker-compose.dev.yml up -d
```

如果用 Maven 或 IDEA 启动后端，可以把开发配置复制为 `.env`，让 Spring Boot 自动读取：

```powershell
Copy-Item .env.dev .env
.\mvnw.cmd spring-boot:run
```

开发环境默认：

- 后端端口：`5090`
- 前端开发地址：`http://localhost:8080`
- 文件访问域名：`http://localhost:5090/files`
- 激活 Profile：`dev`

## 3. 正式部署前检查

上线前必须完成以下检查：

- 更换所有已经出现在本地说明、聊天记录或示例中的密码，包括博客账号、MySQL root 密码、数据库用户密码、Redis 密码、`BLOG_JWT_SECRET`。
- 应用连接数据库不要使用 root，使用 `blog_user` 这类最小权限账号。
- `BLOG_CORS_ALLOWED_ORIGINS` 只填写正式前端域名，不要包含 `localhost`。
- `BLOG_FILE_DOMAIN` 填写正式 HTTPS 后端文件访问地址，例如 `https://api.example.com/files`。
- 确认数据库表已经初始化。当前仓库未内置建表迁移脚本，如果是新服务器，需要先准备 SQL 初始化脚本或从旧库导入。
- 确认服务器防火墙只开放 HTTP/HTTPS 和必要 SSH 端口，不开放 MySQL `3306`、Redis `6379`。

生成生产 JWT 密钥：

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

## 4. 正式 Docker 部署流程

生产镜像由 GitHub Actions 构建，推送到 `main` 后会发布：

```text
ghcr.io/lancasteerr/easy-blog-backend:prod
ghcr.io/lancasteerr/easy-blog-frontend:prod
ghcr.io/lancasteerr/easy-blog-backend:sha-提交SHA
ghcr.io/lancasteerr/easy-blog-frontend:sha-提交SHA
```

如果 GHCR 镜像是私有包，服务器先登录 GHCR。PAT 只需要读取包权限，不要写进脚本或提交到仓库：

```bash
echo "你的PAT" | docker login ghcr.io -u Lancasteerr --password-stdin
```

进入服务器上的后端目录：

```bash
cd /path/to/Easy_blog_full/demo_bk
```

复制生产环境变量：

```bash
cp .env.prod.example .env.prod
```

编辑 `.env.prod`，填写真实值：

```bash
nano .env.prod
```

确认 `.env.prod` 里使用生产镜像标签：

```properties
BLOG_BACKEND_IMAGE=ghcr.io/lancasteerr/easy-blog-backend:prod
BLOG_FRONTEND_IMAGE=ghcr.io/lancasteerr/easy-blog-frontend:prod
```

拉取镜像并启动：

```bash
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml pull
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml up -d
```

查看服务状态：

```bash
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml ps
```

查看后端日志：

```bash
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml logs -f backend
```

停止服务：

```bash
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml down
```

只更新并重启后端：

```bash
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml pull backend
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml up -d backend
```

## 5. 反向代理建议

生产 Compose 中后端只在 Docker 内部网络暴露，由前端 nginx 统一反代 `/api` 和 `/files`。如果服务器外层还有 Nginx、Caddy、宝塔或云负载均衡，建议把 HTTPS 流量转发到前端容器暴露的 `FRONTEND_HTTP_PORT`。

反向代理至少处理这些事项：

- HTTPS 证书和自动续期。
- `/`、`/api/`、`/files/` 都转发到前端容器发布在宿主机上的 HTTP 端口，例如 `http://127.0.0.1:80`。
- 限制上传请求体大小，与 `BLOG_UPLOAD_MAX_REQUEST_SIZE` 保持一致。
- 对隐藏管理入口增加 IP 白名单、VPN、Cloudflare Access 或 Basic 二次认证。
- 设置真实客户端 IP 头，并确保外网不能绕过反向代理直连后端。

## 6. 上线验证

部署后按顺序验证：

```bash
curl -i https://your-backend-domain.example/api/public/get_article_list?page=1\&size=10
```

预期公开文章列表能正常返回。

```bash
curl -i https://your-backend-domain.example/api/admin/content/article
```

预期无 Token 访问管理接口被拒绝。

登录验证：

```bash
curl -i -X POST https://your-backend-domain.example/api/public/login \
  -H "Content-Type: application/json" \
  -d '{"userName":"your-user","password":"your-password"}'
```

预期返回 token。随后用 `Authorization: Bearer <token>` 验证管理接口。

上传验证：

- 小图片应上传成功。
- 超过 `BLOG_UPLOAD_MAX_FILE_SIZE` 的文件应失败。
- 非图片文件不应作为文章图片使用。

## 7. 备份与维护

建议定期备份：

- MySQL 数据卷 `mysql-data`。
- 上传文件数据卷 `blog-storage`。
- `.env.prod` 的离线密钥副本。

上传文件在后端容器内保存到 `/app/blog-storage`，并由 Docker named volume 持久化；不会出现在源码目录 `demo_bk/blog-storage`。如果部署时使用 `-p easy-blog-prod`，本地模拟生产的卷名通常是 `easy-blog-prod_blog-storage`。

查看已保存文件：

```bash
docker exec easy-blog-prod-backend-1 find /app/blog-storage -maxdepth 6 -type f
```

示例数据库备份：

```bash
docker compose --env-file .env.prod -f docker-compose.prod.pull.yml exec mysql \
  mysqldump -u root -p white_jotter > white_jotter_backup.sql
```

恢复或迁移前，先停后端写入流量，避免备份过程中出现数据不一致。

## 8. 后续建议

- 给数据库表补索引，重点是用户表用户名、文章发布时间、浏览量排序、文件临时状态清理字段。
- 增加 Flyway 或 Liquibase，把建表和升级 SQL 纳入版本管理。
- 增加后端健康检查端点，方便 Docker 和反向代理判断服务状态。
- 给上传接口补文件类型白名单，拒绝 `svg/html/js` 等可能造成 XSS 的文件类型。
- 如果以后部署多个后端实例，需要给定时任务增加分布式锁，避免重复刷浏览量或重复清理文件。
